# 新版系统验证记录

验证批次：北京时间 2026-09-13 至 2026-09-14。公开资料采集，已获授权调用真实 DeepSeek 服务。

## 功能验证

- 47 项 Python 测试通过，覆盖初筛、无分类配额、展示优先级、分类、批次隔离、翻译内容与相邻顺序、缓存污染、手动证据隔离、新流程与历史兼容函数。
- 3 项微信格式测试通过，覆盖摘要、缺日报与密钥遮蔽。
- 新版日报审查、静态页面生成与差异格式检查通过。
- 模拟验证了模型协议与指定模型名、伪造引用拒绝、全部来源失败、部分失败、空结果、多于 20 条同源线索、跨天变更、个人反馈隔离。
- 真实模型调用完成：130 条原始线索保留，规则初筛 40 条，DeepSeek 最终 40 条全部通过证据校验；20 条“观察”中按决策价值展示前 15 条，其余分析仍保留。私人笔记与手动输入均不发往模型。
- 初次批量实测只有 12 条通过、28 条未完成。定位为一条校验失败会丢弃整个四条批次；改为二分重试后，第二轮 38 条通过、2 条因引用无法逐字核对被拒绝，第三轮修正引用与翻译后 40 条全部通过。
- 英文引用在前、中文翻译在后；翻译字段必须包含中文。发现并修复一条中文原文被反向译成英文仍通过校验的问题，历史缓存复用前也会重新校验。
- 中文来源现在标为“中文原文”，不再错误显示为“英文原文”；摘要拆分显示规则候选、有效模型分析和日报展示数量，避免模型未配置时把候选量写成已分析量。
- 浏览器阻止本地文件页面访问，因此未完成视觉截图检查；已完成 HTML 生成和原始链接的程序检查。

## 真实采集

第二轮共 130 条：Hugging Face 模型 20、Spaces 20、GitHub 30、Hacker News 30、Product Hunt 30。Reddit 返回 HTTP 错误。122 条有非空且非默认模板的上下文，8 条缺正文或只有配置模板；这不是 122 条已验证需求。

来源单次取样上限用于控制网络开销，不作为日报名额分配。搜索量、服务订单和竞品付费/Roadmap 未自动接入，手动公开证据导入可用。

## 内容抽查与优化

### MiniCPM5-2B

来源：https://huggingface.co/openbmb/MiniCPM5-2B

模型卡描述端侧与资源受限部署。这只能支持能力方向，不能证明有人会为新工具付费，也没有验证实际设备性能。初次提取混入样式代码，已增加 HTML 文本解析与回归测试，再采集后样式噪声消失。

### short-video-generator-AI

来源：https://github.com/Colafornia/short-video-generator-AI

README 描述视频切片、字幕等流程，且已经提供本地网页界面。不能直接推荐“加网页界面”作为缺口。值得继续查的是目标创作者的实际交付障碍、现有竞品与部署成本；是否值得做产品目前未知。

### tokentab

来源：https://github.com/crwdla/tokentab

README 描述本机模型用量与成本统计，说明已有网页面板，并注明某些 provider 尚未完成。不能仅凭开源项目存在推断没有面板或存在付费需求。若继续判断，应核查缺失 provider 的真实需求、用户现有办法与付费意愿。

### 默认 Space 模板

部分 Space 的 README 只有配置参考链接，没有功能说明。已增加识别，保留为候选，不送模型据此编造商业分析。

## 是否适合用户评判

当前输出已适合用户阅读原文、中文翻译、判断理由、未知事项和下一步，并据此标记哪些方向值得继续。19 条均为“观察”，没有把厂商自述、下载量或单次评论冒充已验证市场；这也说明当前公开来源更擅长发现新技术线索，尚不足以稳定产出强付费需求。

内容抽查表明 tokentab、sloppy、AI 聊天记录导出和 AI 写作抱怨等任务较具体；泛模型发布只适合作能力观察。是否真正符合用户兴趣仍以用户反馈为准，不因模型与测试通过就认定机会有商业价值。

最终展示排序优先明确抱怨、工作流中断、已有产品/服务证据和可直接验证的 GitHub 工具；纯模型卡不会因篇幅长或功能词多占满日报。该排序仍不设置分类名额。

其他未完成范围：跨来源语义合并仍需人工复核；个人记录当前为本机命令行，无跨设备 UI；未自动接入的付费数据来源不能冒充已覆盖。线上部署和微信送达以 GitHub Actions 实际结果为准。

## 云端验证

实现提交 `49d7662` 已推送 GitHub。工作流运行 `34767675536` 成功，单元测试、真实采集、审查、Pages 部署、微信模板推送与自动提交均为 success，异常通知步骤正常跳过。微信结果代表接口接受发送，不能证明手机已阅读。

已读取公开页面 `https://sunlixiao8.github.io/alpha-demand-mining/daily/2026-09-14.html`，确认新版日期、来源状态和原始链接已发布。云端自动提交 `0f414aa` 已同步本地。

提交 `4c7c428` 的工作流 `34769274767` 整体成功，采集、审查、Pages、微信与提交步骤均为 success。线上页面确认新筛选与翻译结构已发布，但 GitHub 未配置 `DEEPSEEK_API_KEY`，该次云端运行只复用历史分析，不能写成真实云端模型调用成功；本地真实 DeepSeek 验证已完成。
